apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: my-app
spec:
  containers:
    - name: my-container1
      image: nginx:latest
      ports:
        - containerPort: 80
    - name: my-container2
      image: tomcat
      ports:
        - containerPort: 8080
